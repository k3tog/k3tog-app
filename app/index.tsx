import Onboarding from '@/components/onboarding';

const App = () => {
  return (
    // TODO: need to handle skipping onboarding screen if users have already seen it
    // https://www.youtube.com/watch?v=SLbay2efwso
    <Onboarding />
  );
};

export default App;
